version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: CGono.NagBot.TG.Api/Dockerfile
    environment:
      - ASPNETCORE_URLS=http://+:80
    ports:
      - "80:80"
    restart: always